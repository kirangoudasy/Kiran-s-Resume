5.1.1	Spring Boot sample code
The sample main file which allows to start our spring boot application. This application can be built as a runnable JAR and can be executed.

@SpringBootApplication(scanBasePackages={"com.bcrs.*"})
@EnableConfigurationProperties({FileStorageProperties.class,CbAdjProperties.class,GoFileStorageProperties.class,XmlMasProperties.class})
@PropertySource({"classpath:sme-${spring.profiles.active}.properties","classpath:cbadj-${spring.profiles.active}.properties","classpath:adj-${spring.profiles.active}.properties","classpath:xmlmas-${spring.profiles.active}.properties"})
@EnableAsync
//@EnableCaching
public class BcrsWebportalApplication extends org.springframework.boot.web.servlet.support.SpringBootServletInitializer {
 
    @Override
    protected SpringApplicationBuilder configure(SpringApplicationBuilder application) {
        return application.sources(BcrsWebportalApplication.class);
    }
    public static void main(String[] args) throws Exception {
        SpringApplication.run(BcrsWebportalApplication.class, args);
    }
      }
