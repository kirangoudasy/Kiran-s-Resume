const procResult = await tx.run(
  `CALL GENERIC_DATA_VALIDATOR(?,?,?,?,?,?)`,
  [
    uuid,
    MODULE_NAME,
    SUB_MODULE_NAME,
    LOAD_TEMPLATE,
    null, // O_STATUS
    null  // O_MESSAGE
  ]
);

const STATUS  = procResult?.OUT?.O_STATUS;
const MESSAGE = procResult?.OUT?.O_MESSAGE;
